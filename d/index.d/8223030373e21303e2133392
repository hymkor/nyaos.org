<<[pin] バージョンアップ>>

<<<CSV エクスポート機能>>>

XML とか YAML とか、JSON とかがもてはやされる昨今。
でも、なんだかんだいって、CSV は堅いよ、スレッガーさん。

最初は csv モジュールを使おうとしたが、改行コードを強制的に CRLF にするため、
標準出力をバイナリ化せねばならぬ等、いろいろ面倒な措置を施さねばならぬ雰囲気だったので、
普通にカンマで区切って print した。

<<<驚愕の事実>>>

Python 2.4 のドキュメンより

6<
http://www.python.jp/doc/release/tut/node10.html#SECTION0010600000000000000000{
try 文には、一個以上の except 節か、または一個の finally 節を持たねばなりませんが、'''両方持つことはできません'''。}
>9

Python 2.5 では、この両方持てるようになったらしく、ローカルでは、
両方持つ try文 がエラーにならず、通っていた。

<<<そのままライブラリ化>>>

8<
if __name__ == '__main__':
   main()
>8

という一文を入れたので、他のプログラムからも
import pin すれば、今回の資産をそのまま流用可能。