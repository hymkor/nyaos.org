!!! 概要

Python 3 で、print関数の括弧が嫌な人向けのクラス。
C++ の ostream のような << 演算子が使える。

* Python 2 でも使える。
* print()関数と同様に、要素と要素の間には ' ' 空白が入る
* 改行は endl で行う。<< "\n" とすると、前後に空白が入ってしまう。

!!! ソース

<<{ostream.py}{ダウンロード}

((ref_text ostream.py))

!!! 参考

((ls [Python]*))